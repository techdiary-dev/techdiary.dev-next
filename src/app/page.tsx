import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Home",
};

const page = () => {
  return (
    <div>
      <h1>আমার নাম রায়হান</h1>
    </div>
  );
};

export default page;
